import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    // Dev-only: proxies /api to the backend so the frontend can call
    // relative paths with no CORS setup. Production wiring (reverse proxy
    // or CORS) is deferred to the deployment milestone.
    proxy: {
      '/api': {
        target: 'https://localhost:5001',
        changeOrigin: true,
        secure: false,
      },
    },
  },
})
